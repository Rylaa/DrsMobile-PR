# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DrsMobile' do
pod 'Alamofire', '~> 5.0.0-rc.2'
pod 'Moya', '~> 14.0.0-beta.2'
pod 'SwiftLint'
pod "TinyConstraints"
pod 'TextFieldEffects'
pod 'RadioGroup'
  # Pods for DrsMobile

  target 'DrsMobileTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DrsMobileUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
